# Security Policy

本仓库是公开的 Codex 安装仓库，只能存放插件清单、只读分析说明、公开应用关联和公开安装文档。GitHub 仓库可见性不得作为数据访问控制手段。

禁止提交数据库地址、IP、端口、用户名、密码、证书、隧道 ID、隧道密钥、API 密钥、广告原始数据或客户数据。远程 MCP 必须只暴露查询工具，并使用数据库层面的独立只读角色。

发现任何凭据或写入工具暴露时，应立即撤销相关凭据、停止分发并修复后再发布。

仓库中的固定 `.app.json` 技术 ID 不是跨账号凭据，也不能授予其他个人账号访问权；它仅作为负责人现有连接的恢复映射。面向运营分发时必须先使用无绑定个人版安装器，为目标个人账号生成专属应用映射。不得通过公开仓库共享 Tunnel runtime key 或任何可绕过个人授权的令牌。
